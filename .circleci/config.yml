version: 2
jobs:
  sfdx:
    docker:
      - image: rawdee/sfdx:latest
    steps:
      - checkout
      - run: echo "Check out the repository"
      - run: echo "docker version"
      - run: sfdx --version

workflows:
  version: 2
  sfdx-cicd:
    jobs:
      - sfdx